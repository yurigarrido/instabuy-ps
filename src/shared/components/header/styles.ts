import { styled } from "../../../../stitches.config";

export const Container = styled('header', {
  width: '100%',
  backgroundColor: '$blue1',

  height: '$header',

  position: "fixed",
  top: '0'
})