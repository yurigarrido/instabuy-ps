export const generateBannerUrl = (url: string): string => {
  return `https://assets.instabuy.com.br/ib.store.banner/bnr-${url}`
}

export const generateImageUrl = (url: string): string => {
  return `https://assets.instabuy.com.br/ib.item.image.medium/m-${url}`
}
